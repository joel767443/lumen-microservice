name:laravel
on:
  push:
    branches: [ "main", "develop" ]
  jobs:
    builds: runs-on:ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: build the docker image
        run: docker build . --file Dockerfile --tag image-name:$(date + %s)


